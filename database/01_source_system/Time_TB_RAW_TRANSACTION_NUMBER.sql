CREATE TABLE IF NOT EXISTS source_system.TB_RAW_TRANSACTION_NUMBER (
    "Order ID"     VARCHAR(1000),
    "Ship Date"    VARCHAR(100),
    "Units Sold"   VARCHAR(100),
    "Unit Price"   VARCHAR(100),
    "Unit Cost"    VARCHAR(100)
);